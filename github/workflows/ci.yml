name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: install
        run: npm install
        working-directory: backend

      - name: test
        run: npm test
        working-directory: backend
